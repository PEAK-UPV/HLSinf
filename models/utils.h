/*
 * utils.h
 *
 */

// defines
#define false 0
#define true  1

// prototype functions
int max(int a, int b);
int min(int a, int b);
size_t fn_get_item_line(char *line, size_t len, size_t offset, char *item);
